name: 'Lifecycle Timer'
description: 'Times workflow execution through all pre, main, and post steps'
inputs:
  job_name:
    description: 'Name of the job for artifact naming'
    required: true
runs:
  using: 'node20'
  pre: 'pre.js'
  main: 'main.js'
  post: 'post.js'